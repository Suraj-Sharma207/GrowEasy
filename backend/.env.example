# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000

# API Configuration
API_PREFIX=/api
API_VERSION=v1

# Optional AI Config (graceful fallback applies if missing)
GEMINI_API_KEY=
